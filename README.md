#  SQL – MySQL

- This folder contains a comprehensive collection of SQL problems and queries that I practiced using MySQL, covering both fundamental and intermediate concepts required for data analyst and backend roles.
- The work here reflects my hands-on learning approach, where I focused on writing queries to retrieve, filter, transform, and analyze data from relational databases using real-world–style datasets.
- To strengthen my understanding:
- I solved problems from platforms such as HackerRank, LeetCode, and DataLemur.
- I also used LLMs (ChatGPT, Gemini, Claude) as learning aids to:
- Clarify complex SQL concepts
- Debug query errors
- Optimize and refactor queries
- Learn alternative and more efficient approaches
- All queries were executed, tested, and refined by me to ensure conceptual clarity and correctness.

## 📚 Topics Covered
### 1️⃣ Basic Querying & Data Retrieval
- **SELECT** – Retrieve specific columns or all columns from a table  
- **FROM** – Specify the source table  
- **Column Aliases** – Rename columns for better readability using `AS`  
- **DISTINCT** – Remove duplicate records from query results  

---

### 2️⃣ Filtering Data
- **WHERE Clause** – Filter rows based on conditions  
- **Comparison Operators** – `=`, `!=`, `<`, `>`, `<=`, `>=`  
- **BETWEEN** – Filter values within a specified range  
- **IN / NOT IN** – Match values from a given list  
- **LIKE** – Pattern matching using `%` and `_`  
- **IS NULL / IS NOT NULL** – Handle missing values  

---

### 3️⃣ Logical Conditions
- **AND / OR / NOT** – Combine multiple conditions  
- Proper use of **parentheses** to control condition precedence  

---

### 4️⃣ Sorting & Limiting Results
- **ORDER BY** – Sort results in `ASC` or `DESC` order  
- **LIMIT** – Restrict the number of rows returned  
- **OFFSET** – Skip a specified number of rows (pagination logic)  

---

### 5️⃣ Aggregate Functions & Grouping
- **COUNT, SUM, AVG, MIN, MAX** – Perform aggregations  
- **GROUP BY** – Group rows for summary calculations  
- **HAVING** – Filter aggregated results (used after `GROUP BY`)  

---

### 6️⃣ Joins (Combining Tables)
- **INNER JOIN** – Matching records in both tables  
- **LEFT JOIN** – All records from the left table with matching records from the right  
- **RIGHT JOIN** – All records from the right table with matching records from the left  
- **SELF JOIN** – Join a table with itself  
- Understanding **join conditions and cardinality**  

---

### 7️⃣ Subqueries
- **Scalar Subqueries** – Return a single value  
- **Row & Table Subqueries** – Return multiple rows  
- Subqueries used in:
  - `SELECT`
  - `WHERE`
  - `FROM`
- **Correlated Subqueries** – Basic use cases  

---

### 8️⃣ Set Operations
- **UNION / UNION ALL** – Combine results from multiple queries  
- Difference between `UNION` and `UNION ALL`  

---

### 9️⃣ String, Date & Numeric Functions
- **String Functions** – `UPPER`, `LOWER`, `TRIM`, `SUBSTRING`, `LENGTH`  
- **Date Functions** – `DATE`, `YEAR`, `MONTH`, `DAY`, `DATEDIFF`  
- **Numeric Functions** – `ROUND`, `CEILING`, `FLOOR`  

---

### 🔟 Case & Conditional Logic
- **CASE WHEN** – Apply conditional logic within SQL queries  
- Business-oriented use cases:
  - Categorization  
  - Flag creation  
  - Status labeling  

---

### 1️⃣1️⃣ Data Quality & Handling Missing Values
- Identifying **NULL values**  
- Replacing NULLs using **COALESCE / IFNULL**  
- Understanding the impact of NULLs on aggregations  

---

### 1️⃣2️⃣ Database Design Basics
- Understanding **Primary Keys & Foreign Keys**  
- Basic **Normalization** concepts  
- Importance of **relationships between tables**  

---

## 🎯 Purpose of This Folder
- Build strong **SQL foundations** for data analytics roles  
- Practice **interview-oriented SQL problems**  
- Develop confidence in writing **clean, readable, and optimized queries**  
- Create a **transparent and honest learning portfolio** showcasing real effort and growth  
